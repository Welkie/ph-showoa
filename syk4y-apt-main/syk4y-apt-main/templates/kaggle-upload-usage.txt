Upload artifact datasets to Kaggle with per-artifact change detection.

Usage:
  syk4y kaggle upload [--repo-root DIR] [options] [artifact...]

Options:
  -u, --upload-dir DIR  Upload root (default: kaggle_upload under repo root)
  --force               Force upload even if no changes detected
  -m, --message TEXT    Kaggle version message override
  --dir-mode MODE       Kaggle dir mode: skip|zip|tar|store (store = zip without compression)
  --build-wheel-only    Build/update wheelhouse.zip only, skip Kaggle upload
  --arch ARCH           Target architecture for wheelhouse: native|x86_64|aarch64 (default: native)
  --wheel-arch ARCH     (Deprecated) Alias for --arch
  --repo-root DIR       Target repository root (default: current directory)
  -h, --help            Show this help

Artifacts:
  Optional artifact names/paths (same input style as syk4y init).
  Examples: syk4y kaggle upload datasets, syk4y kaggle upload wheelhouse.
  If omitted, upload checks all initialized artifacts under upload root.
